const base = 'DashBoard';

export const DashBoardActions = {
	SetImportContent: `${base}.SetImportContent`,
	OnImporting: `${base}.OnImporting`,
	OnDimmed: `${base}.OnDimmed`,
	OnReports: `${base}.OnReports`,
	Loading: `${base}.Loading`,
	Error: `${base}.Error`,
};
