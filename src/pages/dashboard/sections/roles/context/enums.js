const namespace = 'Roles';

export const RolesActionTypes = {
	loadRoles: `${namespace}.load`,
	loadPermissions: `${namespace}.loadPermissions`,
	loading: `${namespace}.loading`,
	Error: `${namespace}.Error`,
};
