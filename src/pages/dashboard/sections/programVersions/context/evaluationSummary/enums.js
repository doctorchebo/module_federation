const namespace = 'ProgramVersionEvaluationSummary';

export const EvaluationSummaryActionTypes = {
	loadProgramVersion: `${namespace}.load.programVersion.evaluation`,
	getStages: `${namespace}.load.stages`,
	loadScholars: `${namespace}.load.scholars`,
	loading: `${namespace}.loading`,
	sendEvaluation: `${namespace}.sendEvaluation`,
	error: `${namespace}.Error`,
};
